# CLAUDE.md

This project keeps its agent guidance tool-agnostic so it works equally in Claude
Code and Google Antigravity. The shared brief lives in `AGENTS.md` — read it first.

@AGENTS.md

## Claude Code specifics

- No build or test commands exist yet — this repo is docs + (soon) static mockups.
- When creating the mockups, prefer plain HTML/CSS/JS with a shared `tokens.css`
  so each mockup opens with a double-click and needs no server. Revisit tooling
  only once Ellie has picked a direction and `docs/PRD.md` settles the real stack.
- Start every working session by skimming `docs/open-questions.md` — it is the
  live list of things the coordinators still need to decide.
